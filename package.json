{
  "name": "meditrack",
  "version": "1.0.0",
  "description": "Hospital Management System",
  "private": true,
  "scripts": {
    "start": "cd server && npm start",
    "dev": "cd server && npm run dev",
    "client": "cd meditrack-client && npm start",
    "build": "cd meditrack-client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../meditrack-client && npm install"
  },
  "keywords": [
    "hospital",
    "management",
    "medical",
    "health"
  ],
  "author": "Milka Hospital Team",
  "license": "ISC",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
